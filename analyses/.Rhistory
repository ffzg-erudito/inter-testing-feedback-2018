describe(dataset)
library(haven)
install.packages(haven)
install.packages("psych")
install.packages("haven")
library(haven)
library(psych)
path = file.path("C:/Users/matpa/Downloads/", "CFA18_2.sav")
dataset = read_sav(path)
describe(dataset)
library(haven)
install.packages("haven")
library(haven)
library(psych)
path = file.path("C:/Users/matpa/Downloads/", "CFA18_2.sav")
dataset = read_sav(path)
describe(dataset)
names(dataset)
summary(dataset)
str(dataset)
library(lavaan)
install.packages("lavaan")
library(lavaan)
?HolzingerSwineford1939
install.packages("ggplot")
install.packages("ggplot2", dependencies = TRUE)
?ggplor
?ggplot
?ggplot2
library(ggplot2)
?ggplot
?ggplot2
library(lavaan)
c = 1 2 3 4
c = [1 2 3 4]
c = [1,2,3,4,5]
c = (1,2,3,4,5)
c = (1 2 3 5 6)
v = c(1, 2, 3 ,4 ,5)
v = c(0,0,0,0,0)
v = c(1, 2, 3 ,4 ,5)
d = c(0,0,0,0,0)
cor(v,d)
cor(x,y, methode = "spearman")
cor(x,y, method = "spearman")
cor(v,d, method = "spearman")
sd(d)
moj_vec = c(1, 2,3,4,5)
moj_vec
library(here)
install.packages("here")
library(here)
# NOTE: this will load {magrittr}, {here}, {conflicted} and {tidyverse}. also,
# `conflict_prefer`s filter from {dplyr}
# furthermore, it loads 3 data.frames: (1) `dat` which contains the pooled data run
# through `2-wrangling-main.R`, (2) `datHard` which is `dat` with all the hard
# exclusion criteria applied (as described in `analysis-plan.md`), and (3)
# `datSoft` which is `datHard` with the soft exclusion criteria applied (as
# described in `analysis-plan.md`)
source(here('wrangling', '3-exclusion-criteria.R'))
library(here)
# NOTE: this will load {magrittr}, {here}, {conflicted} and {tidyverse}. also,
# `conflict_prefer`s filter from {dplyr}
# furthermore, it loads 3 data.frames: (1) `dat` which contains the pooled data run
# through `2-wrangling-main.R`, (2) `datHard` which is `dat` with all the hard
# exclusion criteria applied (as described in `analysis-plan.md`), and (3)
# `datSoft` which is `datHard` with the soft exclusion criteria applied (as
# described in `analysis-plan.md`)
source(here('wrangling', '3-exclusion-criteria.R'))
setwd
setwd(C:(/Users/matpa/Documents/karijera/posao/e-rudito/repos/inter-testing-feedback-2018)
setwd('C:/Users/matpa/Documents/karijera/posao/e-rudito/repos/inter-testing-feedback-2018')
library(here)
# NOTE: this will load {magrittr}, {here}, {conflicted} and {tidyverse}. also,
# `conflict_prefer`s filter from {dplyr}
# furthermore, it loads 3 data.frames: (1) `dat` which contains the pooled data run
# through `2-wrangling-main.R`, (2) `datHard` which is `dat` with all the hard
# exclusion criteria applied (as described in `analysis-plan.md`), and (3)
# `datSoft` which is `datHard` with the soft exclusion criteria applied (as
# described in `analysis-plan.md`)
source(here('wrangling', '3-exclusion-criteria.R'))
setwd('C:/Users/matpa/Documents/karijera/posao/e-rudito/repos/inter-testing-feedback-2018/analyses')
library(here)
# NOTE: this will load {magrittr}, {here}, {conflicted} and {tidyverse}. also,
# `conflict_prefer`s filter from {dplyr}
# furthermore, it loads 3 data.frames: (1) `dat` which contains the pooled data run
# through `2-wrangling-main.R`, (2) `datHard` which is `dat` with all the hard
# exclusion criteria applied (as described in `analysis-plan.md`), and (3)
# `datSoft` which is `datHard` with the soft exclusion criteria applied (as
# described in `analysis-plan.md`)
source(here('wrangling', '3-exclusion-criteria.R'))
library(here)
# NOTE: this will load {magrittr}, {here}, {conflicted} and {tidyverse}. also,
# `conflict_prefer`s filter from {dplyr}
# furthermore, it loads 3 data.frames: (1) `dat` which contains the pooled data run
# through `2-wrangling-main.R`, (2) `datHard` which is `dat` with all the hard
# exclusion criteria applied (as described in `analysis-plan.md`), and (3)
# `datSoft` which is `datHard` with the soft exclusion criteria applied (as
# described in `analysis-plan.md`)
source(here('wrangling', '3-exclusion-criteria.R'))
